DEDUÇÕES MUNICÍPIOS - XX = 
IF(
    CALCULATE(
        SUM(RECEITA[REC_VAL_REALIZADO_DEDU_MUNICIPIO]),
        RECEITA[REC_MES] = {X}
    ) = 0,
    0.00,
    CALCULATE(
        SUM(RECEITA[REC_VAL_REALIZADO_DEDU_MUNICIPIO]),
        RECEITA[REC_MES] = {X}
    )
)
